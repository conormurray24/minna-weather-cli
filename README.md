# Minna Weather CLI

A simple command-line tool that fetches current weather for any city. Built to practice working with REST APIs and JSON data.

## Installation
1. Clone the repo.
2. Install dependencies: `pip install -r requirements.txt`

## Usage
```bash
python weather.py "Minna"
python weather.py "Lagos"
python weather.py "London"
